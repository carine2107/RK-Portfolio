import { NotFoundContent } from '@/components/layout/NotFoundContent'

/** Localised 404 for `notFound()` in a page. Rendered inside the locale layout. */
export default function NotFound() {
  return <NotFoundContent />
}
