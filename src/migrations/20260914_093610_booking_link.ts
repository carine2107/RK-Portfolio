import { MigrateUpArgs, MigrateDownArgs, sql } from '@payloadcms/db-postgres'

export async function up({ db, payload, req }: MigrateUpArgs): Promise<void> {
  await db.execute(sql`
   ALTER TABLE "site_settings" ADD COLUMN "booking_url" varchar;
  ALTER TABLE "site_settings_locales" ADD COLUMN "booking_label" varchar;`)
}

export async function down({ db, payload, req }: MigrateDownArgs): Promise<void> {
  await db.execute(sql`
   ALTER TABLE "site_settings" DROP COLUMN "booking_url";
  ALTER TABLE "site_settings_locales" DROP COLUMN "booking_label";`)
}
